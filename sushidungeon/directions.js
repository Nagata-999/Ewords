'use strict';
(function(){
  // Direction classes are produced by the stable dungeon renderer.
  // Enemy artwork is now owned by enemy_sprites.js so this file must not
  // repaint the cave rat and cause legacy SVG flashes between frames.
  window.addEventListener('DOMContentLoaded',()=>{});
})();
